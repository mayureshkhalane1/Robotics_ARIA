# Testing & Validation Checklist – `apartment.wbt`

## ✅ Load Verification
- [ ] Open Webots R2025a.
- [ ] File → Open World → `src/webots/worlds/apartment.wbt`.
- [ ] If prompted to convert to W3D, click **Yes** (or **No**, both work).
- [ ] Verify no fatal errors appear in the console (warnings are OK).

## 🦾 Robot Presence
- [ ] Verify the ARIA robot (gray box) appears in the viewport.
- [ ] Confirm the robot's camera placeholder (small black box) is visible.

## 🎯 Searchable Objects
- [ ] Cup (red cylinder) at (2,0,2).
- [ ] Football (green sphere) at (-1,0,3).
- [ ] Bottle (blue cylinder) at (0,0,4).
- [ ] Plant (green cylinder) at (3,0,1).
- [ ] Ensure each object has a distinct colour and is not overlapped.

## 💡 Lighting
- [ ] Verify the overhead point light illuminates the scene (objects not overly dark).

## 📷 Camera Feed (via UI)
- [ ] Run `./scripts/run_webots.sh`.
- [ ] In another terminal, start the UI server: `uv run python -m src.ui.server`.
- [ ] Open the dashboard at `http://127.0.0.1:8080`.
- [ ] Confirm the live camera feed shows the robot’s perspective (should see floor and objects).

## 🚦 Interaction Test (Optional)
- [ ] Using the UI, set a goal: `find cup`.
- [ ] Click **Run** and watch the Smart Vision Agent reasoning.
- [ ] Verify the agent reports seeing the cup and attempts to move towards it (movement not implemented in this pure‑VRML robot – you’ll see a planning step).

## 🐞 Troubleshooting Guide (see `FAQ.md`)
- If the world fails to load, confirm the header is `#VRML_SIM R2023b utf8`.
- If objects are missing, check that each `DEF` block includes a `boundingObject`.
- If the robot does not appear, ensure the `ARIA_ROBOT` Solid block is present.
- If the camera feed is black, verify Ollama is running and the LLM can process images.

---
*Generated by the Jcode Agent on 2026‑05‑18.*
